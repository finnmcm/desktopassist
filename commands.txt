1. CONFIGURE (only do when changing the top level CMakeLists.txt):

cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug

2. COMPILE (the one you use all the time to re compile the project)

cmake --build build -j

3. RUN THE PROJECT 

cmake --build build --target run

**BETTER WAY TO RUN THE PROJECT WITH ARGS**
./build/Debug/bin/macmanager


CLEANING THE BUILD FOLDER

cmake --build build --target clean
cmake --build build -j


Additionally, if build folder is cluttered, just run:
rm -rf build
mkdir build

DB stored at:
~/Library/Application\ Support/MacAssistant/
- open with: 
sqlite3 "$HOME/Library/Application Support/MacAssistant/state.db"


